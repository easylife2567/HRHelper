# HR Helper 项目技术文档

## 1. 项目概述
HR Helper 是一款面向HR的智能化辅助工具，旨在提升招聘流程的效率和体验。项目深度集成了 AI 能力与现代化 Web 技术，提供从**简历智能解析**、**人才库可视化管理**到**个性化面试题生成**的一站式解决方案。

后端采用 **飞书多维表格 (Feishu Bitable)** 作为核心数据库，确保了数据的灵活性与协同性；前端采用 **Arco Design**构建了美观、流畅的用户界面。

## 2. 核心功能模块

### 📊 仪表盘 (Dashboard)
*   **数据可视化**: 使用 `Recharts` 展示候选人评分分布（柱状图）和招聘状态分布（饼图）。
*   **快捷入口**: 提供常用功能的快速导航。
*   **实时统计**: 实时显示人才库总数、待面试、已通过等关键指标。

### 👥 人才库 (Talent Pool)
*   **双视图切换**:
    *   **列表视图 (Table)**: 支持复杂的筛选、搜索、排序及分页查看，适合批量管理。
    *   **看板视图 (Kanban)**: **[新增]** 采用 Trello 风格的拖拽交互，直观展示“待面试”、“面试中”、“已通过”、“已淘汰”流转状态。支持**乐观更新 (Optimistic UI)**，拖拽操作零延迟。
*   **全流程管理**: 支持候选人的增删改查 (CRUD)，数据实时同步至飞书多维表格。
*   **邮件集成**: 支持直接对候选人发送自定义邮件（如面试邀约、拒绝信）。

### 📄 简历智能评估 (Resume Evaluation)
*   **AI 深度解析**: 上传 PDF 简历，调用 Coze Workflow 进行多维度分析。
*   **智能评分**: 根据预设维度自动打分，并生成结构化的评估报告。
*   **后台异步处理**: 支持后台静默分析，分析完成后自动更新状态。

### ❓ 个性化面试题 (Interview Questions)
*   **智能生成**: 基于候选人简历内容，自动生成针对性的面试问题及考察意图。
*   **去重与排序**: 自动按邮箱去重，并根据候选人优先级（S/A/B级）智能排序。
*   **交互式浏览**: 采用侧边栏+详情卡的布局，方便快速切换查看不同候选人的面试题。

### 📧 邮件定制 (Email Customization)
*   **模板管理**: 提供可定制的邮件模板。
*   **参数替换**: 支持动态替换候选人姓名、职位等信息。

## 3. 技术栈架构

### 3.1 前端 (Client)
*   **核心框架**: React 18 + TypeScript + Vite 5
*   **UI 组件库**: `@arco-design/web-react` (字节跳动 Arco Design)
*   **状态管理**: `Zustand` (极简且强大的状态管理)
*   **路由管理**: `React Router DOM v6`
*   **数据可视化**: `Recharts`
*   **交互增强**: `@dnd-kit` (实现高性能的看板拖拽)
*   **Markdown 渲染**: `react-markdown` + `remark-gfm`
*   **HTTP 请求**: Axios

### 3.2 后端 (Server)
*   **运行时**: Node.js
*   **Web 框架**: Express v5
*   **语言**: TypeScript
*   **核心服务**:
    *   **Feishu Service**: 对接飞书 Open API，读写 Bitable 数据。
    *   **Coze Service**: 对接 Coze API，触发 AI 工作流。
    *   **Email Service**: 基于 Nodemailer 的邮件发送服务。
    *   **File Service**: Multer 处理文件上传。

### 3.3 数据流向
1.  **用户操作** (前端 React) -> **API 请求** (Axios) -> **Node.js Server** (Express)
2.  **Server** -> **Feishu Open Platform** (数据持久化)
3.  **Server** -> **Coze API** (AI 推理与分析)

## 4. 快速启动 (Getting Started)

### 环境要求
*   Node.js >= 18
*   npm

### 配置说明
在 `server` 目录下创建 `.env` 文件，填入以下配置：
```env
PORT=3000
FEISHU_APP_ID=your_app_id
FEISHU_APP_SECRET=your_app_secret
FEISHU_APP_TOKEN=your_app_token
FEISHU_TABLE_ID=your_table_id
COZE_API_KEY=your_coze_api_key
COZE_BOT_ID=your_coze_bot_id
```

### 启动服务端
```bash
cd server
npm install
npx ts-node src/index.ts
# 服务将运行在 http://localhost:3000
```

### 启动客户端
```bash
cd client
npm install
npm run dev
# 页面将运行在 http://localhost:5173
```

## 5. 项目结构
```
HRHelper/
├── client/                 # 前端项目
│   ├── src/
│   │   ├── pages/          # 页面组件 (TalentPool, Dashboard...)
│   │   ├── components/     # 通用组件 (KanbanBoard...)
│   │   ├── store.ts        # Zustand 状态管理
│   │   └── ...
│   └── ...
├── server/                 # 后端项目
│   ├── src/
│   │   ├── services/       # 业务逻辑 (Feishu, Coze, Email)
│   │   ├── routes/         # API 路由
│   │   └── ...
│   └── ...
└── README.md
```
