# HR Helper 项目技术文档

## 1. 项目概述
HR Helper 是一款面向HR的辅助工具，旨在提升简历筛选、人才管理及沟通效率。项目集成了 Coze AI Workflow 进行简历智能分析，使用飞书多维表格（Feishu Bitable）作为后端数据库，提供从简历解析到面试邀约的全流程支持。

## 2. 技术栈架构

### 2.1 前端 (Client)
*   **核心框架**: React 18 + TypeScript + Vite 5
*   **路由管理**: React Router DOM v6
*   **状态管理**: Zustand (用于全局用户状态、Sidebar状态及人才列表缓存)
*   **UI 组件库**:
    *   `@douyinfe/semi-ui`: 主要 UI 框架，用于布局及大部分组件。
    *   `@arco-design/web-react`: 亦被引入项目中，用于部分组件（存在混合使用的情况）。
*   **HTTP 请求**: Axios
*   **Markdown 渲染**: react-markdown + remark-gfm (用于展示 AI 分析结果)

### 2.2 后端 (Server)
*   **运行时**: Node.js
*   **Web 框架**: Express v5
*   **语言**: TypeScript (开发) -> JavaScript (运行)
*   **文件处理**: Multer (处理简历上传)
*   **核心服务集成**:
    *   **Coze Service**: 调用 Coze API 执行简历分析 Workflow。
    *   **Feishu Service**: 对接飞书多维表格 API，实现候选人数据的 CRUD 操作。
    *   **Email Service**: Nodemailer (目前为 Mock 实现)。

### 2.3 数据流
1.  **简历分析**: 用户上传 PDF -> Express (Multer) -> 上传至 Coze -> 触发 Coze Workflow -> 返回分析 JSON -> 前端展示。
2.  **人才库管理**: 前端操作 -> Express API -> Feishu API -> 读写飞书多维表格。
3.  **认证**: 简单的 Mock 登录机制。

## 3. 核心功能模块

| 模块 | 描述 | 关键文件 |
| :--- | :--- | :--- |
| **Dashboard** | 仪表盘首页，提供快捷入口和概览 | `DashboardHome.tsx` |
| **Resume Evaluation** | 上传简历，调用 AI 进行评分和解析 | `ResumeEvaluation.tsx`, `coze.ts` |
| **Talent Pool** | 候选人列表管理，支持增删改查 | `TalentPool.tsx`, `feishu.ts` |
| **Email Customization** | 自定义并发送邮件通知 | `EmailCustomization.tsx`, `email.ts` |
| **Interview Questions** | (规划中) 生成面试题 | `InterviewQuestions.tsx` |

---

## 4. 现有问题与改进建议 (Issues & Recommendations)

通过对代码库的静态分析，发现以下潜在问题和优化点：

### 🛑 高优先级 (Critical/High)

1.  **UI 组件库混合使用 (Mixed UI Libraries)**
    *   **现状**: `package.json` 同时引入了 `Semi Design` 和 `Arco Design`。
    *   **问题**: 导致打包体积膨胀；由两套设计系统导致 UI 风格不统一（字体、圆角、交互行为差异）。
    *   **建议**: 统一使用一套 UI 库（建议根据历史记录保留 Arco 或 Semi 其中之一，目前 `main.tsx` 使用了 Semi 的 `LocaleProvider`，倾向较大）。

2.  **硬编码配置 (Hardcoded Configuration)**
    *   **现状**: `client/src/store.ts` 中硬编码了 API 地址 `http://localhost:3000/api/talent/list`。
    *   **问题**: 无法在非本地环境（生产/测试环境）部署，修改端口需改代码。
    *   **建议**: 使用 Vite 的环境变量 `import.meta.env.VITE_API_URL` 进行配置。

3.  **Mock 认证与安全 (Mock Auth)**
    *   **现状**: `MainController.login` 使用硬编码的 `admin/admin` 进行判断。
    *   **问题**: 仅适用于演示，毫无安全性。
    *   **建议**: 接入真实的飞书 SSO 登录或完善 JWT/Session 认证机制。

### ⚠️ 中优先级 (Medium)

4.  **废弃文件残留 (Dead Code)**
    *   **现状**: `server/candidates.json` 存在但未被逻辑引用（目前逻辑已切至 Feishu）。
    *   **建议**: 删除该文件以避免误导。

5.  **Mock 邮件服务**
    *   **现状**: `MainController.sendCustomEmail` 仅打印日志，未实际调用 `nodemailer`。
    *   **建议**: 在 `email.ts` 中完善 SMTP 配置，实现真实发送。

6.  **TypeScript 类型定义**
    *   **现状**: 前后端类型定义分离，且存在 `any` 类型使用（如 `talentList: any[]`）。
    *   **建议**: 建立共享类型库（shared types）或在前端完善 Interface 定义，提升代码健壮性。

### ℹ️ 低优先级 (Low)

7.  **项目文档缺失**
    *   **现状**: 根目录无 `README.md` 说明启动流程。
    *   **建议**: 补充启动文档（如何配置 .env，如何启动 client/server）。

---

## 5. 启动流程 (推测)

1.  **Server**:
    ```bash
    cd server
    npm install
    # 需配置 .env (PORT, COZE_API_KEY, FEISHU_APP_ID 等)
    npm run start (或 nodemon)
    ```
2.  **Client**:
    ```bash
    cd client
    npm install
    npm run dev
    ```
